# Build artifacts, regenerated by the builder stage. Including them breaks
# `make clean` in the image, which runs as a different uid than the host user.
build/
bin/
test/

# Go build and module caches, if BUILD_CACHE_DIR is pointed inside the tree.
.cache/
.config/
